# NTP Monitor Configuration
import os

# NTP Server Configuration
NTP_SERVER = "my.ntp-server.lan"
POLLING_INTERVAL = 60  # seconds
MAX_POINTS = 1000

# InfluxDB Configuration
INFLUX_CONFIG = {
    'url': os.getenv('INFLUX_URL', 'http://my_influxdb_ip:8086'),
    'token': os.getenv('INFLUX_TOKEN', my_influxdb_token'),
    'org': os.getenv('INFLUX_ORG', 'my_influxdb_org'),
    'bucket': os.getenv('INFLUX_BUCKET', 'ntp-monitoring')
}

# File Paths
DATA_DIR = "/opt/ntp-monitor/data"
CSV_FILE = os.path.join(DATA_DIR, "ntp_metrics.csv")
PLOT_FILE = os.path.join(DATA_DIR, "ntp_metrics.png")

# Logging Configuration
LOG_LEVEL = "INFO"
LOG_FILE = "/var/log/ntp-monitor/ntp-monitor.log"
